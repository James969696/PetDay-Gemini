rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Use signed URLs issued by backend; deny direct public access.
      allow read, write: if false;
    }
  }
}
