rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Backend API mediates access; direct client reads/writes are disabled by default.
      allow read, write: if false;
    }
  }
}
